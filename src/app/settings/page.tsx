"use client";
import { FormEvent,useEffect,useState } from "react";
import { Header } from "@/components/layout/Header";
export default function Settings(){const [name,setName]=useState('');const [email,setEmail]=useState('');const [voice,setVoice]=useState('');const [notice,setNotice]=useState('');useEffect(()=>{fetch('/api/settings').then(r=>r.json()).then(j=>{const s=j.data?.settings;if(s){setName(s.userName||'');setEmail(s.userEmail||'');setVoice(s.brandVoice||'')}})},[]);async function save(e:FormEvent){e.preventDefault();const r=await fetch('/api/settings',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({userName:name,userEmail:email,brandVoice:voice})});setNotice(r.ok?'Settings saved.':'Could not save settings.')};return <div className="flex-1"><Header title="Settings" subtitle="Profile and writing preferences"/><main className="mx-auto max-w-2xl p-8"><form onSubmit={save} className="space-y-5 rounded-2xl border border-slate-200 bg-white p-7"><Field label="Name"><input className="input" value={name} onChange={e=>setName(e.target.value)} required/></Field><Field label="Email"><input className="input" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></Field><Field label="Writing guidance"><textarea className="input min-h-32" value={voice} onChange={e=>setVoice(e.target.value)} placeholder="Describe how SignalNest should write for you." required minLength={10}/></Field><div className="flex items-center justify-between"><p className="text-sm text-slate-500">{notice}</p><button className="rounded-lg bg-indigo-600 px-4 py-2.5 text-sm font-medium text-white">Save settings</button></div></form></main><style jsx>{`.input{width:100%;border:1px solid #cbd5e1;border-radius:.6rem;padding:.7rem .8rem;color:#0f172a;outline:none}.input:focus{border-color:#6366f1;box-shadow:0 0 0 3px #e0e7ff}`}</style></div>}
function Field({label,children}:{label:string;children:React.ReactNode}){return <label className="block"><span className="mb-2 block text-sm font-medium text-slate-700">{label}</span>{children}</label>}
