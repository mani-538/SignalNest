"use client";
import { useEffect, useState } from "react";
import Link from "next/link";
import { CalendarDays, ArrowRight } from "lucide-react";
import { Header } from "@/components/layout/Header";
import { ContentItem } from "@/types";

export default function CalendarPage(){const [items,setItems]=useState<ContentItem[]>([]);const [loading,setLoading]=useState(true);useEffect(()=>{fetch("/api/content").then(r=>r.json()).then(j=>setItems(j.data??[])).finally(()=>setLoading(false))},[]);return <div className="flex-1"><Header title="Calendar" subtitle="Your drafts and scheduled content"/><main className="mx-auto max-w-4xl p-8">{loading?<p className="text-sm text-slate-500">Loading…</p>:items.length===0?<section className="rounded-2xl border border-dashed border-slate-300 bg-white px-6 py-16 text-center"><CalendarDays className="mx-auto h-6 w-6 text-slate-400"/><h2 className="mt-4 font-semibold">Nothing scheduled</h2><p className="mt-2 text-sm text-slate-500">Drafts and scheduled posts you create will appear here.</p><Link href="/studio" className="mt-5 inline-flex items-center gap-1 text-sm font-medium text-indigo-600">Create a draft <ArrowRight className="h-4 w-4"/></Link></section>:<div className="space-y-3">{items.map(i=><article key={i.id} className="rounded-xl border border-slate-200 bg-white p-5"><div className="flex items-center justify-between"><h2 className="font-medium">{i.title||i.coreIdea}</h2><span className="rounded-full bg-slate-100 px-2 py-1 text-xs text-slate-600">{i.status}</span></div><p className="mt-2 text-sm text-slate-600">{i.platform}{i.scheduledAt?` · ${new Date(i.scheduledAt).toLocaleString()}`:" · Not scheduled"}</p></article>)}</div>}</main></div>}
