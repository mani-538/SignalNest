"use client";
import { useEffect,useState } from "react";
import { Github,CheckCircle2,Clock3 } from "lucide-react";
import { Header } from "@/components/layout/Header";
import { SocialConnection } from "@/types";
export default function Integrations(){const [items,setItems]=useState<SocialConnection[]>([]);useEffect(()=>{fetch('/api/integrations').then(r=>r.json()).then(j=>setItems(j.data??[]))},[]);return <div className="flex-1"><Header title="Integrations" subtitle="Connect accounts through their official APIs"/><main className="mx-auto max-w-4xl space-y-6 p-8"><section className="rounded-xl border border-slate-200 bg-white p-5"><div className="flex items-center justify-between gap-4"><div className="flex items-center gap-3"><div className="rounded-lg bg-slate-900 p-2 text-white"><Github className="h-5 w-5"/></div><div><h2 className="font-medium">GitHub</h2><p className="text-sm text-slate-500">Repositories, issues, pull requests and releases</p></div></div>{items.find(i=>i.provider==='github')?.status==='connected'?<span className="flex items-center gap-1 text-sm text-emerald-600"><CheckCircle2 className="h-4 w-4"/>Connected</span>:<a href="/api/auth/github" className="rounded-lg bg-indigo-600 px-4 py-2 text-sm font-medium text-white">Connect</a>}</div></section><section><h2 className="mb-3 text-sm font-medium text-slate-700">Planned integrations</h2><div className="grid gap-3 sm:grid-cols-2">{items.filter(i=>i.provider!=='github').map(i=><div key={i.provider} className="flex items-center justify-between rounded-xl border border-slate-200 bg-white p-4"><span className="capitalize text-sm font-medium">{i.provider}</span><span className="flex items-center gap-1 text-xs text-slate-500"><Clock3 className="h-3.5 w-3.5"/>Pending</span></div>)}</div></section></main></div>}
