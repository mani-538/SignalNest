"use client";
import { FormEvent, useState } from "react";
import { Header } from "@/components/layout/Header";
import { PlatformType } from "@/types";

const platforms: PlatformType[]=["linkedin","github","instagram","facebook","x","youtube","tiktok"];
export default function StudioPage(){const [idea,setIdea]=useState("");const [draft,setDraft]=useState("");const [platform,setPlatform]=useState<PlatformType>("linkedin");const [title,setTitle]=useState("");const [saving,setSaving]=useState(false);const [notice,setNotice]=useState("");async function save(e:FormEvent){e.preventDefault();setSaving(true);setNotice("");const r=await fetch("/api/content",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({coreIdea:idea,platform,title,draft,status:"draft",hashtags:[],tone:"Thought Leadership",targetAudience:"Audience"})});const j=await r.json();setSaving(false);if(r.ok){setIdea("");setDraft("");setTitle("");setNotice("Draft saved.")}else setNotice(typeof j.error==="string"?j.error:"Could not save draft.")};return <div className="flex-1"><Header title="Content" subtitle="Write and save your own drafts"/><main className="mx-auto max-w-3xl p-8"><form onSubmit={save} className="space-y-5 rounded-2xl border border-slate-200 bg-white p-7"><Field label="Platform"><select value={platform} onChange={e=>setPlatform(e.target.value as PlatformType)} className="input">{platforms.map(p=><option key={p}>{p}</option>)}</select></Field><Field label="Title"><input className="input" value={title} onChange={e=>setTitle(e.target.value)} placeholder="Optional title"/></Field><Field label="Idea"><textarea className="input min-h-24" value={idea} onChange={e=>setIdea(e.target.value)} placeholder="What do you want to communicate?" required minLength={3}/></Field><Field label="Draft"><textarea className="input min-h-64" value={draft} onChange={e=>setDraft(e.target.value)} placeholder="Write your post…" required/></Field><div className="flex items-center justify-between"><p className="text-sm text-slate-500">{notice}</p><button disabled={saving} className="rounded-lg bg-indigo-600 px-4 py-2.5 text-sm font-medium text-white disabled:opacity-50">{saving?"Saving…":"Save draft"}</button></div></form></main><style jsx>{`.input{width:100%;border:1px solid #cbd5e1;border-radius:.6rem;padding:.7rem .8rem;background:white;color:#0f172a;outline:none}.input:focus{border-color:#6366f1;box-shadow:0 0 0 3px #e0e7ff}`}</style></div>}
function Field({label,children}:{label:string;children:React.ReactNode}){return <label className="block"><span className="mb-2 block text-sm font-medium text-slate-700">{label}</span>{children}</label>}
