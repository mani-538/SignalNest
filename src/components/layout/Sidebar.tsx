"use client";
import Link from "next/link";
import { usePathname } from "next/navigation";
import { LayoutDashboard,Inbox,PenLine,CalendarDays,BarChart3,Plug,Settings } from "lucide-react";
const links=[['Home','/',LayoutDashboard],['Inbox','/inbox',Inbox],['Content','/studio',PenLine],['Calendar','/calendar',CalendarDays],['Analytics','/analytics',BarChart3],['Integrations','/integrations',Plug],['Settings','/settings',Settings]] as const;
export function Sidebar(){const path=usePathname();return <aside className="flex h-screen w-60 shrink-0 flex-col border-r border-slate-200 bg-white"><Link href="/" className="border-b border-slate-200 px-6 py-5"><span className="text-lg font-semibold tracking-tight text-slate-950">SignalNest</span><p className="mt-0.5 text-xs text-slate-500">Social workspace</p></Link><nav className="space-y-1 p-3">{links.map(([name,href,Icon])=>{const active=href==='/'?path===href:path.startsWith(href);return <Link key={href} href={href} className={`flex items-center gap-3 rounded-lg px-3 py-2.5 text-sm ${active?'bg-indigo-50 font-medium text-indigo-700':'text-slate-600 hover:bg-slate-50 hover:text-slate-900'}`}><Icon className="h-4 w-4"/>{name}</Link>})}</nav><div className="mt-auto border-t border-slate-200 p-4 text-xs text-slate-500">Private workspace</div></aside>}
